// import { fromJS } from 'immutable';
// import { makeSelectViewPracticeDomain } from '../selectors';

// const selector = makeSelectViewPracticeDomain();

describe('makeSelectViewPracticeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
