// import { fromJS } from 'immutable';
// import { makeSelectExamDisplayDomain } from '../selectors';

// const selector = makeSelectExamDisplayDomain();

describe('makeSelectExamDisplayDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
