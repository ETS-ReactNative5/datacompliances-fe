// import { fromJS } from 'immutable';
// import { makeSelectViewExamDomain } from '../selectors';

// const selector = makeSelectViewExamDomain();

describe('makeSelectViewExamDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
