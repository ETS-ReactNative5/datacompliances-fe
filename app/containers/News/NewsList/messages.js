import { defineMessages } from 'react-intl';

export const scope = 'app.containers.News.NewsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewsList container!',
  },
});
