import { defineMessages } from 'react-intl';

export const scope = 'app.containers.News.NewsDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewsDetails container!',
  },
});
