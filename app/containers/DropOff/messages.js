import { defineMessages } from 'react-intl';

export const scope ='app.containers.DropOff';

export default defineMessages({
    headers: {
        id: `${scope}.header`,
        defaultMessage: 'This is the DropOff container!',
    },
});