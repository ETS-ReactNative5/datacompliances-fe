import { defineMessages } from 'react-intl';

export const scope ='app.containers.Financial';

export default defineMessages({
    headers: {
        id: `${scope}.header`,
        defaultMessage: 'This is the Financial container!',
    },
});