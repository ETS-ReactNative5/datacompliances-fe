import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Finance.FinancialDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FinancialDetail container!',
  },
});
