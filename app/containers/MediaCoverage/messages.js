import { defineMessages } from 'react-intl';

export const scope ='app.containers.MediaCoverage';

export default defineMessages({
    headers: {
        id: `${scope}.header`,
        defaultMessage: 'This is the MediaCoverage container!',
    },
});