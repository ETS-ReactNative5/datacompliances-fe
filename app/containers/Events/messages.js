import { defineMessages } from 'react-intl';

export const scope ='app.containers.Events';

export default defineMessages({
    headers: {
        id: `${scope}.header`,
        defaultMessage: 'This is the Event container!',
    },
});