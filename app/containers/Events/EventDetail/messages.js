import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Events.EventDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EventDetail container!',
  },
});
